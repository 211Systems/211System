@{
    ViewData["Title"] = "Rejestracja Remizy";
}

<div class="row">
    <div class="col-md-6 offset-md-3">
        <div class="card card-danger card-outline dark-mode">
            <div class="card-header">
                <h3 class="card-title">Wprowadź dane nowej remizy</h3>
            </div>
            <form id="createDepartmentForm">
                <div class="card-body">
                    <div class="form-group">
                        <label>Nazwa Placówki</label>
                        <input type="text" id="deptName" class="form-control" required placeholder="np. JRG nr 1">
                    </div>
                    <div class="form-group">
                        <label>Adres</label>
                        <input type="text" id="deptAddress" class="form-control" required placeholder="np. ul. Polna 1">
                    </div>
                    <div class="form-group">
                        <label>Region / Rejon</label>
                        <input type="text" id="deptRegion" class="form-control" required placeholder="np. Śródmieście">
                    </div>
                    <p id="error-message" class="text-danger d-none"></p>
                </div>
                <div class="card-footer text-right">
                    <a href="/Fire/Home" class="btn btn-secondary">Anuluj</a>
                    <button type="submit" class="btn btn-danger" id="btn-submit">Zarejestruj</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.getElementById('createDepartmentForm').addEventListener('submit', async function(e) {
            e.preventDefault();
            const btn = document.getElementById('btn-submit');
            const errorMsg = document.getElementById('error-message');
            
            btn.disabled = true;
            errorMsg.classList.add('d-none');

            const payload = {
                name: document.getElementById('deptName').value,
                address: document.getElementById('deptAddress').value,
                region: document.getElementById('deptRegion').value
            };

            try {
                const token = localStorage.getItem('jwt');
                const response = await fetch('/api/Fire/departments', {
                    method: 'POST',
                    headers: {
                        'Authorization': 'Bearer ' + token,
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(payload)
                });

                if (response.ok) {
                    window.location.href = '/Fire/Home';
                } else {
                    errorMsg.textContent = "Błąd rejestracji placówki. Sprawdź uprawnienia.";
                    errorMsg.classList.remove('d-none');
                    btn.disabled = false;
                }
            } catch (error) {
                errorMsg.textContent = "Błąd komunikacji z serwerem.";
                errorMsg.classList.remove('d-none');
                btn.disabled = false;
            }
        });
    </script>
}